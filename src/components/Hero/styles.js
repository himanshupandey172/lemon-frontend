import theme from "../../theme";

const styles = {
  container: (theme) => ({
    display: "flex",
    flexDirection: "column",
    justifyContent: "center",
    alignItems: "center",
    padding: "1rem",
  }),

  mainHeadingContainer: (theme) => ({
    display: "flex",
    flexDirection: "column",
    justifyContent: "center",
  }),
};

export default styles;
